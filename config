parentdir="$(dirname `pwd`)"
project=`basename $parentdir`

export BORG_PASSPHRASE=$project
compression="lz4"
backupuser="borgbackup"
backupserver="localhost"
backupport="9022"
backupdir="/home/borgbackup/"

mountpoint="$HOME/mnt/$project"
